const readline = require("readline").createInterface
({
    input: process.stdin,
    output: process.stdout
});
readline.question("get the year from console",(input) => {
    readline.question("get the year from console",(input2) => {
        readline.question("get the year from console",(input3) => {
        let sum = a + b * c;
        let sum2 = c + a / b;
        let sum3 = a % b + c;
        let sum4 = a * b + c;
        console.log(sum);
        console.log(sum2);
        console.log(sum3);
        console.log(sum4);
});
    });
});